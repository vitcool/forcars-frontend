import { FormatYearPipe } from './format-year.pipe';

describe('FormatYearPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatYearPipe();
    expect(pipe).toBeTruthy();
  });
});
